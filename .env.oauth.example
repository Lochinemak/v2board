# OAuth Configuration Example for V2Board
# Copy these settings to your .env file and configure with your OAuth provider details

# Application URL (required for OAuth callbacks)
APP_URL=https://your-domain.com

# GitHub OAuth Configuration
# Create an OAuth App at: https://github.com/settings/applications/new
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
# Authorization callback URL: https://your-domain.com/api/v1/passport/oauth/github/callback

# Google OAuth Configuration  
# Create credentials at: https://console.developers.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
# Authorized redirect URI: https://your-domain.com/api/v1/passport/oauth/google/callback

# Custom OAuth Provider Configuration
# Replace with your actual OAuth provider details
OAUTH_CLIENT_ID=your_oauth_client_id
OAUTH_CLIENT_SECRET=your_oauth_client_secret
OAUTH_PROVIDER_NAME="Your OAuth Provider"
# Redirect URI: https://your-domain.com/api/v1/passport/oauth/oauth_provider/callback

# Optional: Default settings for new OAuth users
# V2BOARD_REGISTER_PLAN_ID=1
# V2BOARD_COMMISSION_DEFAULT_TYPE=0
# V2BOARD_COMMISSION_DEFAULT_RATE=10
# V2BOARD_REGISTER_DEFAULT_TRANSFER=100
